// Найти среднее значение последовательности чисел с помощью reduce

"use strict"

const arrr = [1, 4, 4, 10];

console.log( arrr.reduce((acc,elem) => acc + elem, 0) / arrr.length )

abc = "23"

console.log(abc)